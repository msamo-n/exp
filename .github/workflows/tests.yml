name: Tests
on:
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Fake Tests
        run: |
          sleep 3
          echo 'I am OK with that'
